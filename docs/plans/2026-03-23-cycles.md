# Cycles Implementation Plan

**Goal:** Generalize all main results from single curves to cycles (formal ℤ-linear combinations).

## Type: `ContourCycle := PiecewiseC1Immersion →₀ ℤ`

Using `Finsupp` for algebraic structure + mathlib API.

## New file: `Cycle.lean`

### Definitions
- `ContourCycle` (abbrev for Finsupp)
- `contourIntegralCycle f Γ = Γ.sum (fun γ n => n * ∮_γ f)`
- `windingNumberCycle Γ z = Γ.sum (fun γ n => n * n(γ,z))`
- `IsNullHomologousCycle Γ U` (each closed, each in U, winding zero outside)

### Theorems
1. Cauchy for cycles: `contourIntegralCycle_eq_zero_of_nullHomologous`
2. Residue theorem for cycles (simple poles)
3. Residue theorem for cycles (higher-order, A'+B)
4. Winding number integrality for cycles

### Bridge lemmas
- `_single`, `_add`, `_neg` for each operation
- `isNullHomologousCycle_single`, `_add`

All proofs: linearity of Finsupp.sum + single-curve theorems.
