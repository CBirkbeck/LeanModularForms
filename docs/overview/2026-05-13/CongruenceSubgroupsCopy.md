# CongruenceSubgroupsCopy.lean

### File Summary
Essentially empty — file contains only header docstring. Deprecated local copy of definitions for congruence subgroups (`Γ(N)`, `Γ₀(N)`, `Γ₁(N)`). All declarations have been upstreamed to mathlib at `Mathlib.NumberTheory.ModularForms.CongruenceSubgroups`. File is retained only as an empty placeholder to preserve the import path.

- **What**: Empty placeholder for upstreamed congruence subgroup definitions.
- **Uses-from-project**: [].
- **Used by**: [] (import path only).
- **Visibility**: none.
- **Lines**: 15.
- **Notes**: Pure placeholder — no declarations.
