# Marathon recon notes — 2026-05-11

## What this session accomplished

Strategic recon of the SMO codebase against the reviewer's 2026-05-11
guidance.  Refined understanding of which sorries are tractable, what
infrastructure already exists, and the minimal residual work for each
critical-path ticket.  **No new code was written**; existing tickets and
plan were refined for a clean next-session handoff.

## Pre-existing infrastructure (much more than tickets suggested)

### T205-d ladder — largely DONE

The reviewer's "first theorem boundary"
(`isFundamentalDomain_iUnion_smul_of_finiteIndex`) is **already proved**
in two forms:

- **Abstract**: `MeasureTheory.IsFundamentalDomain.subgroup_iUnion_out_smul`
  in `PeterssonLevelN.lean:304`.  Generic version for any subgroup of any
  group with an outer fundamental domain.

- **Specialized**: `Gamma_p_α_PSL_R_FD_finite_index_decomp_auto` in
  `AdjointTheory.lean:1596`.  Targets the projective ambient PSL(2,ℝ),
  with `Gamma_p_α = conjGL Γ₁(N) α ⊓ Γ₁(N)` as the conjugate-intersection
  subgroup.  All Countable / Fintype instances are automatic.  No ±I
  kernel obstruction.

The reviewer's "second theorem boundary"
(`petN_doubleCoset_adjoint`) has its **FD-shift adapter** also proved:
`Gamma_p_α_PSL_R_FD_finite_index_decomp_shifted` in
`AdjointTheory.lean:1667`.

The remaining work for T205-d is to discharge the **explicit Prop**
`DSDoubleCosetTileBridge` (defined at AdjointTheory.lean:8159), which
captures the precise aggregate-tile identity needed.  A PROVED consumer
`petN_heckeT_p_adjoint_standard_form_of_doubleCosetTileBridge`
(line 8228) closes the DS 5.5.3 standard form via one `rw` once
`DSDoubleCosetTileBridge` is provided.

### POST-4 (mainLemma) — different shape than reviewer expected

The codebase has the **reduction**
`mainLemma_of_newSubspace_coprime_vanishing_zero` (Newforms.lean:2519,
PROVED) which reduces `mainLemma` to a zero-criterion on
`cuspFormsNew`.

**Critical finding**: the reduction's required hypothesis (that any
`g ∈ cuspFormsNew` with coprime-vanishing coefficients is zero) is
exactly what the reviewer's proposed Miyake-direct route would prove —
but with the same descent obstacle.

### Miyake 4.6.8 infrastructure — substantial but at deep level

Existing in `Eigenforms/MainLemma.lean`:
- `miyake_main_lemma_4_6_8_level_L` (line 1221, PROVED)
- `exists_oldform_pieces_decomposition_of_coprime_prod_vanish`
  (line 2483, PROVED)
- `exists_oldform_pieces_decomposition_charSpace_qSupp_of_coprime_prod_vanish`
  (line 2883, PROVED)
- `same_level_collapse_of_deep_oldform_image` (line 2966) — takes the
  same-level descent as a `h_bridge` HYPOTHESIS

All produce decompositions at the **deep level** `Γ₁(M·L.prod)`.  The
descent back to level `Γ₁(M)` is the T132-T134 obligation, the
"genuine remaining gap".

### Prime-power mainLemma — CLOSABLE

For `N = p^r`, the codebase has:
- `mainLemma_charSpace_primePower_via_divisor_iSup`
  (AtkinLehner.lean:1395, PROVED) — char-space version
- `mem_iSup_divisor_qSupportedOnDvdSubmodule_inf_charSpace_of_coprime_vanishing_primePower`
  (line 1314, PROVED) — the membership step

Wrapping this for the non-character version of `mainLemma` at prime
power level is a **mechanical character-decomposition wrap** using
`exists_finsupp_charSpace_of_diamondOpCuspHom_invariant`
(CharacterDecomp.lean:843, PROVED).

### T207 — has substantial scaffolding

The current sorry at `AdjointTheory.lean:18235` already has:
- `heckeFamily_joint_eigenspace_top` (line 18007, PROVED)
- `heckeFamily_isFinitelySemisimple` (line 18033, PROVED) — does the
  χ-twist trick for a single operator
- `joint_eigenspace_mem_isCommonEigenfunction` (line 18131, PROVED)
- `eigenforms_orthogonal_of_distinct_eigenvalues` (line 18176, PROVED)
- `petN_innerProductCore` (line 17738) — InnerProductSpace.Core

The proof is a Mathlib-API assembly via:
- `Module.End.iSupIndep_iInf_maxGenEigenspace_of_forall_mapsTo`
  (or its joint-eigenspace analog in `Eigenspace/Pi.lean`)
- `DirectSum.isInternal_submodule_of_iSupIndep_of_iSup_eq_top`
- `DirectSum.IsInternal.collectedBasis`
- Gram-Schmidt within each W_ev for same-eigenvalue orthogonality

The whole proof is **on top of proved infrastructure** — no new
analytic content needed.

## Refined critical path

```
Track 1 — Adjoint/spectral (high-effort):
  T205-d-API-1 ✅ → T205-d-API-2 (DSDoubleCosetTileBridge)
                 → T205-d ← consumer is PROVED
                 → T207 (~300 LOC of Mathlib assembly)

Track 2 — mainLemma:
  Prime-power case: CLOSABLE in 50-100 LOC (mechanical wrap)
  Composite case: requires TraceDescent witness (NEW infra,
                  ~500-1000 LOC for one of trace/AL-Li/U_p route)

Track 3 — SMO (POST-7):
  Once mainLemma + T207 land: ~80 LOC of multiplicativity cancellation
```

## Next-session priorities (in order of tractability)

### P1: Prime-power mainLemma wrap (~100 LOC, low risk)

Add to `Newforms.lean` a wrapped version of `mainLemma` for prime-power
levels.  This does NOT close the existing sorry at line 2563 (general
N), but adds usable API and proves the Miyake-direct route works for
the simpler case.

Statement sketch:
```
theorem mainLemma_primePower
    {p : ℕ} [hp : Fact p.Prime] {r : ℕ} (hr : 0 < r) {k : ℤ}
    (f : CuspForm ((Gamma1 (p^r)).map (mapGL ℝ)) k)
    (h_vanish : ∀ n : ℕ, Nat.Coprime n (p^r) →
      (ModularFormClass.qExpansion (1 : ℝ) f).coeff n = 0) :
    f ∈ cuspFormsOld (p^r) k
```

Proof: decompose `f = Σ_χ f_χ` via
`exists_finsupp_charSpace_of_diamondOpCuspHom_invariant`, apply
`mainLemma_charSpace_primePower_via_divisor_iSup` to each piece, sum
back.

### P2: T207 closure (~300 LOC, medium risk)

Plan: see task #6 description.  Pure Mathlib linear-algebra packaging
on proved infrastructure.  Result is conditional on T205-d closing
(via `heckeT_n_adjoint_on_charSpace` dependency on T205-d).

### P3: DSDoubleCosetTileBridge (~500-1000 LOC, high risk)

The combinatorial bijection at the heart of T205-d.  Reviewer
suggested the σ-reindex approach via `Gamma1QuotEquivOfGamma0`.
Significant infrastructure already in place at lines 8255+ of
AdjointTheory.lean (e.g. `petN_diamond_heckeT_p_symm_RHS_sum_distributed_reindex`).

### P4: Composite-N mainLemma via TraceDescent (~1000+ LOC, very high risk)

Requires constructing the `TraceDescent` witness via one of:
(a) refined trace operator with cusp-stabilizer correction (T124 gap)
(b) Atkin-Lehner-Li Petersson orthogonality (needs T205-d + T207)
(c) U_p-eigenspace decomposition at level N

Route (b) is most natural given existing infrastructure.

## Lessons for the reviewer's future feedback

1. The reviewer's "T205-d-API-1 should be the narrowest usable form"
   guidance is in fact already followed — the existing
   `Gamma_p_α_PSL_R_FD_finite_index_decomp_auto` is precisely this.
   Documentation gap, not infrastructure gap.

2. The reviewer's claim that "Main Lemma should not depend on T207"
   is correct for the **prime-power** case but misses the composite-N
   same-level descent obstacle.  For composite N, **either** T207
   (via AL-Li orthogonality) **or** a trace-operator construction is
   genuinely required.  The reviewer may have underestimated this.

3. The reviewer's correction about FALSE newform coefficient claim is
   accurate and important — it's properly removed from the plan.

4. The mathematical risks enumerated by the reviewer (Risks 2, 3, 5)
   are well-aligned with the existing codebase's defensive notes (the
   "kernel obstruction" comments at lines 1366-1386 of AdjointTheory).

## State at end of marathon recon

- `tickets.md`: refined per reviewer feedback (commits a59f...)
- `REVIEW_BRIEF.md`: in project root
- `.mathlib-quality/expert-review/2026-05-11/`: full state preserved
- Build: clean (3550 jobs, warnings only)
- No code modifications attempted this session
